// Game balance constants — change here to update all modes simultaneously

/** Delay (ms) before navigating to Results after win/loss */
export const RESULTS_NAVIGATE_DELAY_MS = 1200;

/** Guess threshold to reveal the Status clue (Alive/Deceased) in Casual mode */
export const CLUE_STATUS_AFTER = 5;

/** Guess threshold to reveal the Affiliation clue in Casual mode */
export const CLUE_AFFILIATION_AFTER = 8;
